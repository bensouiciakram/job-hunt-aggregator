---
scope: Job Hunt Aggregator v1 (FR-1..FR-5) + v2 hooks (FR-6..FR-8)
purpose: build substrate for the PRD: engine + application loop v1, scoring/urgency v2
altitude: feature
updated: 2026-08-18T22:35
---

- (decision) Paradigm: hexagonal collector (SourcePort) + filter-chain pipeline behind single-owner Django core; Next.js API-only
- (decision) Scrape layer: JobSpy wraps google adapter; bespoke Scrapy for ouedkniss; Playwright for facebook-groups (SERP workaround)
- (decision) Scheduler: APScheduler in collector worker, 30-min poll, backfill on downtime; SQLite single writer; verified versions Next.js 16, Django 6.1, Python 3.13, JobSpy active
- (assumption) Code-first adapters (module + registry row) over YAML-only configs; repository = ListingRepository wrapping ORM; history = seen_sources set + timestamps
- (change) Review fixes applied: AD-4 rewritten (immutable dedup_fingerprint + unique constraint, status/seen_sources write-protected); AD-1 canonical field set + code-first adapter resolution; AD-2 SQLite WAL/busy_timeout; AD-9 (ordering/paging/new bucket); AD-10 (API contract); APScheduler pinned <4; Google-fragility fallback deferred
- (event) spine finalized
